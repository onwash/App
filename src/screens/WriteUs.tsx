import * as React from 'react';
import { View,Text } from 'react-native';

export interface IWriteUsProps {
}

export default function WriteUs (props: IWriteUsProps) {
  return (
    <View>
      <Text>1</Text>
    </View>
  );
}
